{% extends 'base.html.twig' %}
{% block title %}Inscription{% endblock %}

{% block body %}
    <div class="flex justify-center items-center h-full bg-gray-100 py-12">
        <div class="w-full max-w-md bg-white shadow-md rounded-lg p-8">
            <h1 class="text-2xl font-bold mb-6 text-center text-gray-800">Créer un compte</h1>

            {{ form_start(registrationForm, {'attr': {'class': 'space-y-6'}}) }}

            {% for field in [
                registrationForm.firstName,
                registrationForm.lastName,
                registrationForm.email,
                registrationForm.plainPassword.first,
                registrationForm.plainPassword.second,
                registrationForm.country
            ] %}
                {% set hasError = field.vars.errors|length > 0 %}
                <div>
                    {{ form_label(field, null, {'label_attr': {'class': 'block text-sm font-medium text-gray-700'}}) }}
                    {{ form_widget(field, {
                        'attr': {
                            'class': 'block w-full px-4 py-2 rounded-md shadow-sm focus:ring focus:ring-indigo-200 ' ~
                            (hasError ? 'border-red-500 border' : 'border border-gray-300')
                        }
                    }) }}
                    {% if field.vars.errors|length > 0 %}
                        <div class="text-red-600 text-sm mt-1">
                            {{ form_errors(field) }}
                        </div>
                    {% endif %}
                </div>
            {% endfor %}

            <!-- Termes -->
            <div>
                <div class="flex items-center">
                    {{ form_widget(registrationForm.agreeTerms, {
                        'attr': {'class': 'mr-2'}
                    }) }}
                    <label class="text-sm text-gray-700" for="registration_form_agreeTerms">
                        J’accepte les <a href="{{ path('app_cgu') }}" class="text-indigo-600 underline" target="_blank">CGU / CGV</a>
                    </label>
                </div>
                {{ form_errors(registrationForm.agreeTerms, {'attr': {'class': 'text-red-600 text-sm mt-1'}}) }}
            </div>

            <!-- Liens -->
            <div class="text-sm text-right sm:text-left mt-4">
                <a href="{{ path('app_login') }}" class="text-indigo-600 hover:underline block">Se connecter</a>
                <a href="{{ path('app_forgot_password_request') }}" class="text-indigo-600 hover:underline block mt-1">Mot de passe oublié ?</a>
            </div>

            <!-- Bouton -->
            <button type="submit"
                    class="w-full bg-indigo-600 text-white font-semibold py-2 px-4 rounded hover:bg-indigo-700 transition">
                S'inscrire
            </button>

            {{ form_end(registrationForm) }}
        </div>
    </div>
{% endblock %}
